get:
  summary: 'Get Storage consumption metric over time for the container '
  operationId: getContainerMetricStorage
  parameters:
    - $ref: '../parameters/path/containerId.yaml'
    - $ref: '../parameters/query/lastSeconds.yaml'
  tags:
    - Container Metrics
  responses:
    '200':
      description: 'Storage consumption'
      content:
        application/json:
          schema:
            $ref: '../schemas/MetricStorageResponseList.yaml'
    '401':
      $ref: '../responses/NotAuthorized.yaml'
    '403':
      $ref: '../responses/Forbidden.yaml'
    '404':
      $ref: '../responses/NotFound.yaml'

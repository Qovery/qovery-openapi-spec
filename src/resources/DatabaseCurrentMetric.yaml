get:
  summary: 'Get current metric consumption of the database '
  operationId: getDatabaseCurrentMetric
  parameters:
    - $ref: '../parameters/path/databaseId.yaml'
  tags:
    - Database Metrics
  responses:
    '200':
      description: 'Get database  current metric'
      content:
        application/json:
          schema:
            $ref: '../schemas/DatabaseCurrentMetricResponse.yaml'
    '401':
      $ref: '../responses/NotAuthorized.yaml'
    '403':
      $ref: '../responses/Forbidden.yaml'
    '404':
      $ref: '../responses/NotFound.yaml'



    
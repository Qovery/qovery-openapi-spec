get:
  summary: 'List XX last database service events'
  description: Returns all last XX events. Response is not paginated
  operationId: listDatabaseServiceEventTail
  parameters:
    - $ref: '../parameters/path/databaseServiceId.yaml'
    - $ref: '../parameters/query/tail.yaml'
  tags:
    - databaseService
  responses:
    '200':
      description: 'List events'
      content:
        application/json:
          schema:
            $ref: '../schemas/EventResponseList.yaml'
    '401':
      $ref: '../responses/NotAuthorized.yaml'

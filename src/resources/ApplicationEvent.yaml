get:
  summary: 'List last events'
  description: By default it returns the 20 last results. The result is paginated. In order to request the next page, you can use the startId quesry parameter
  operationId: listApplicationEvent
  parameters:
    - $ref: '../parameters/path/applicationId.yaml'
    - $ref: '../parameters/query/startId.yaml'
  tags:
    - application
  responses:
    '200':
      description: 'List events'
      content:
        application/json:
          schema:
            $ref: '../schemas/EventPaginatedResponseList.yaml'
    '401':
      $ref: '../responses/NotAuthorized.yaml'


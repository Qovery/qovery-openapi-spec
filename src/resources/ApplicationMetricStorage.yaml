get:
  summary: 'Get Storage consumption metric over time for the application'
  operationId: getApplicationMetricStorage
  parameters:
    - $ref: '../parameters/path/applicationId.yaml'
    - $ref: '../parameters/query/lastDays.yaml'
  tags:
    - application
  responses:
    '200':
      description: 'Storage consumption'
      content:
        application/json:
          schema:
            type: object
            properties:
              results:
                type: array
                items:
                  $ref: '../schemas/MetricResponse.yaml'
    '401':
      $ref: '../responses/NotAuthorized.yaml'

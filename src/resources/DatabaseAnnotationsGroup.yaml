get:
  summary: 'List database annotations group'
  description: 'List database annotations group'
  operationId: listDatabaseAnnotationsGroup
  parameters:
    - $ref: '../parameters/path/databaseId.yaml'
  tags:
    - Database Annotations Group
  responses:
    '200':
      description: 'List database annotations groups'
      content:
        application/json:
          schema:
            $ref: '../schemas/OrganizationAnnotationsGroupResponseList.yaml'
    '401':
      $ref: '../responses/NotAuthorized.yaml'
    '403':
      $ref: '../responses/Forbidden.yaml'
    '404':
      $ref: '../responses/NotFound.yaml'

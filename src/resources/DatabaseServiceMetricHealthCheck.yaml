get:
  summary: 'Get Health Check latency  metric over time for the database service'
  description: 'The value returned corresponds to the 95th centile'
  operationId: getDatabaseServiceMetricHealthCheck
  parameters:
    - $ref: '../parameters/path/databaseServiceId.yaml'
    - $ref: '../parameters/query/lastSeconds.yaml'
  tags:
    - databaseService
  responses:
    '200':
      description: 'Health Check Latency'
      content:
        application/json:
          schema:
            type: object
            properties:
              results:
                type: array
                items:
                  $ref: '../schemas/MetricGenericResponse.yaml'
    '401':
      $ref: '../responses/NotAuthorized.yaml'

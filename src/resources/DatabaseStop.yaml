post:
  summary: 'Stop database'
  operationId: stopDatabase
  parameters:
    - $ref: '../parameters/path/databaseId.yaml'
  tags:
    - database
  responses:
    '200':
      description: 'Database  stop has been requested'
    '401':
      $ref: '../responses/NotAuthorized.yaml'
    '409':
      description: 'Database  is already stopped or an operation is in progress'

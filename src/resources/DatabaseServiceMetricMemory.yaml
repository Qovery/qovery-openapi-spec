get:
  summary: 'Get Memory consumption metric over time for the database Service'
  operationId: getDatabaseServiceMetricMemory
  parameters:
    - $ref: '../parameters/path/databaseServiceId.yaml'
    - $ref: '../parameters/query/lastSeconds.yaml'

  tags:
    - databaseService
  responses:
    '200':
      description: 'Memory consumption'
      content:
        application/json:
          schema:
            type: object
            properties:
              results:
                type: array
                items:
                  $ref: '../schemas/MetricMemoryDatapointResponse.yaml'
    '401':
      $ref: '../responses/NotAuthorized.yaml'

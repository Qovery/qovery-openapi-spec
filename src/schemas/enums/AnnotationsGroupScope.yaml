type: string
description: Annotations Group Scope
enum:
    - PERSISTENT_VOLUME_CLAIMS
    - DEPLOYMENTS
    - STATEFUL_SETS
    - SERVICES
    - INGRESS
    - HPA
    - PODS
    - SECRETS
    - JOBS
    - CRON_JOBS

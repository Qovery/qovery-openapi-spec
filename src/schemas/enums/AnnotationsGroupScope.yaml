type: string
description: Annotations Group Scope
enum:
    - PERSISTENT_VOLUME_CLAIMS
    - REPLICA_SETS
    - DEPLOYMENTS
    - STATEFUL_SETS
    - SERVICES
    - INGRESS
    - HPA
    - ENDPOINTS
    - PODS
    - PERSISTENT_VOLUMES
    - SECRETS
    - CONFIG_MAPS
    - JOBS
    - CRON_JOBS

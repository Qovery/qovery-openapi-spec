type: string
enum:
  - ECR
  - SCALEWAY_CR
  - DOCKER_HUB
  - GITHUB_CR
  - GITLAB_CR
  - PUBLIC_ECR
  - DOCR
  - GENERIC_CR
  - GCP_ARTIFACT_REGISTRY
description: The type of your container registry


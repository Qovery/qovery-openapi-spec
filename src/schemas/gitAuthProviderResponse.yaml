allOf:
  - $ref: './BaseResponse.yaml'
  - type: object
    required:
      - name
      - owner
      - credentials
    properties:
      name:
        type: string
        example: "GITHUB"
      owner:
        type: string
        example: "John Doe"

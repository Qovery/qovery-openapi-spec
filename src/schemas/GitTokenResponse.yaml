allOf:
  - $ref: './BaseResponse.yaml'
  - type: object
    required:
      - name
      - type
    properties:
      name:
        type: string
      description:
        type: string
      type:
        $ref: './enums/GitProvider.yaml'

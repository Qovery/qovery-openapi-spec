allOf:
  - $ref: './BaseResponse.yaml'
  - type: object
    properties:
      name:
        type: string
      kind:
        $ref: ../schemas/enums/ContainerRegistryKind.yaml
      description:
        type: string
      url:
        type: string
        description: URL of the container registry      
      belongs_to_cluster:
        type: boolean
        description: If this registry is used by a cluster

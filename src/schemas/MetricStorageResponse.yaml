type: object
required:
  - instance_name
  - data
properties:
  disk_name:
    type: string
    example: disk 1
  data:
    type: array
    items:
      $ref: '../schemas/MetricStorageDatapointResponse.yaml'

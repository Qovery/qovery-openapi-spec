type: object
required:
  - created_at
  - requested_in_mb
  - consumed_in_mb
  - consumed_in_percent
properties:
  created_at:
    type: string
    format: date-time
  requested_in_mb:
    type: integer
    description: unit is MB. 1024 MB = 1GB
  consumed_in_mb:
    type: integer
    description: unit is MB. 1024 MB = 1GB
  consumed_in_percent:
    type: number
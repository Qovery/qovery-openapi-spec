type: object
properties:
  links:
    type: object
    properties:
      previous:
        type: string
        format: uri
      next:
        type: string
        format: uri
  events:
    type: array
    items:
      $ref: './OrganizationEventResponse.yaml'

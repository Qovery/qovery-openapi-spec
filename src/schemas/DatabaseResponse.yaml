allOf:
  - $ref: './BaseResponse.yaml'
  - type: object
    properties:
      environment:
        $ref: './ReferenceObject.yaml'
      uri:
        type: string
      status:
        $ref: './Status.yaml'
  - $ref: './DatabaseRequest.yaml'

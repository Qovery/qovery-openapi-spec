type: object
required:
  - name
  - plan
properties:
  name:
    type: string
    description: name is case insensitive
  description:
    type: string
  plan:
    type: string
    enum:
      - FREE
      - BUSINESS
  website_url:
    type: string
    nullable: true

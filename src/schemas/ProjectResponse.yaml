allOf:
  - $ref: './BaseResponse.yaml'
  - type: object
    required:
      - name
    properties:
      name:
        type: string
      description:
        type: string
      organization:
        $ref: './ReferenceObject.yaml'

allOf:
  - $ref: './BaseResponse.yaml'
  - type: object
    required:
      - name
    properties:
      name:
        type: string
      updated_at:
        type: string
        nullable: true
        format: date-time
      organization:
        $ref: './ReferenceObject.yaml'

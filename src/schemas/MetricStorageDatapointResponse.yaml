type: object
required:
  - created_at
  - requested_in_mb
  - consumed_in_mb
  - consumed_in_percent
properties:
  created_at:
    type: string
    format: date-time
  requested_in_gb:
    type: integer
    description: Unit is in GB.
  consumed_in_gb:
    type: number
    description: Unit is in GB.
  consumed_in_percent:
    type: number
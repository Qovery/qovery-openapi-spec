type: object
properties:
  image_tag:
    type: string
    description: |
      Image tag to deploy.  
      Cannot be set if `git_commit_id` is defined
  git_commit_id:
    type: string
    description: |
      Commit to deploy
      Cannot be set if `image_tag` is defined

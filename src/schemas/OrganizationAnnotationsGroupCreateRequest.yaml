type: object
required:
  - name
  - annotations
  - scopes
properties:
  name:
    type: string
    description: name of the annotations group
  annotations:
    type: array
    items:
      $ref: './Annotation.yaml'
  scopes:
    type: array
    items:
      $ref: "./enums/AnnotationsGroupScope.yaml"

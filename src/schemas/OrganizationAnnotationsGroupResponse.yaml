type: object
required:
  - id
  - name
  - annotations
  - scopes
properties:
  id:
    type: string
    format: uuid
  name:
    type: string
  annotations:
    type: array
    items:
      $ref: './Annotation.yaml'
  scopes:
    type: array
    items:
      $ref: "./enums/AnnotationsGroupScope.yaml"

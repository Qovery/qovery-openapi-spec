type: object
required:
  - routes
properties:
  routes:
    type: array
    items:
      type: object
      required:
        - destination
        - target
      properties:
        destination:
          type: string
        target:
          type: string
        description:
          type: string

type: object
required:
  - created_at
  - requested_in_float
  - consumed_in_number
  - consumed_in_percent
properties:
  created_at:
    type: string
    format: date-time
  requested_in_number:
    type: number
    description: unit is millicores (m). 1000m = 1 cpu
  consumed_in_number:
    type: number
    description: unit is millicores (m). 1000m = 1 cpu
  consumed_in_percent:
    type: number